/**
 * @enum Gender
 */
export enum Gender {
    male = "male",
	female = "female",
	other = "other"
}

export enum Roles {
	admin = "ADMIN",
	driver = "DRIVER",
	rider = "RIDER",
	empty = ""
}

export enum MFAs {
	googleAuth = "Google Authenticator",
	phoneAuth = "Phone Authentication",
	email = "Email Authentication"
}
